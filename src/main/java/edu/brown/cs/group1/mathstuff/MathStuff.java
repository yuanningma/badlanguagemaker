package edu.brown.cs.group1.mathstuff;

public class MathStuff {

  // public static double dot(double[] v1, double[] v2) {
  // if (v1.length != v2.length) {
  // System.err.println("ERROR: The two vectors are of unequal length");
  // }
  //
  // double toret = 0;
  // for (int i = 0; i < v1.length; i++) {
  // toret += v1[i] * v2[i];
  // }
  //
  // return toret;
  // }

  // public static double[] normalize(double[] v) {
  // double magnitude = 0;
  // for (int i = 0; i < v.length; i++) {
  // magnitude += Math.pow(v[i], 2);
  // }
  // magnitude = Math.sqrt(magnitude);
  // if (magnitude != 0) {
  // int l = v.length;
  // double[] res = new double[l];
  // double s = 1 / magnitude;
  // for (int i = 0; i < l; i++) {
  // res[i] = v[i] * s;
  // }
  // return res;
  // } else {
  // return v;
  // }
  // }
}
